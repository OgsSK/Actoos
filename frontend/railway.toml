# Railway Configuration for Actoos Frontend
# This file helps Railway understand how to build and run the frontend

[build]
builder = "nixpacks"
buildCommand = "yarn install && yarn build"

[deploy]
startCommand = "npx serve -s build -l $PORT"
healthcheckPath = "/"
healthcheckTimeout = 100
